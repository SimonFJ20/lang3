
# v = 0..10
fn hyperbole(v) {
    let cond = v < 5;
    let r = 0;
    if cond {
        r = 0;
    } else {
        r = v * 2;
    }
    return r;
}

fn add(a, b) {
    return a + b;
}

fn main() {
    let v = add(2, 2);
    return 0;
}

